/*
  Sanitizer program: checks the test input is in the correct format before
  sending it to the solution.
  Takes the test input on its standard input then gives and exit code of:
    0 if the test input is in the correct format
    1 if it's not or if there's any error

  If you change the path of the sanitizer, execute
    taskstarter.py add sanitizer [path_to_new_sanitizer]
  to update task settings.
*/
